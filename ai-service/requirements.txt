fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.9
pydantic==2.9.2
pydantic-settings==2.5.2
httpx==0.27.2
pytest==8.3.3

# Classic image tools — the "compute with code, not AI" layer (no GPU, no model).
# Color extraction + canonical normalization run entirely on these.
Pillow==10.4.0
numpy==2.1.1

# Fashion embeddings (self-host, no API key). Only loaded when
# EMBED_PROVIDER=fashionsiglip; CPU works (slower). Heavy install (~torch).
open_clip_torch==2.26.1
torch==2.4.1
pillow-heif==1.5.0
