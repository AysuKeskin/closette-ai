# Optional heavy runtime extras — NOT needed for tests or the mock provider.
# Install alongside requirements.txt only when self-hosting embeddings
# (EMBED_PROVIDER=fashionsiglip) or decoding iPhone HEIC photos:
#   pip install -r requirements.txt -r requirements-ml.txt
open_clip_torch==3.3.0
torch==2.4.1
pillow-heif==1.5.0
